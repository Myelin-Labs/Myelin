{
  "schema": "evolving-dob-fixture-v1",
  "fixture": "legacy_version_reject",
  "action": "initialise_dob_state",
  "expected": "rejected",
  "coverage": "source-guard-present",
  "description": "Any intent whose version is not exactly 1 is rejected. DOB-EVO/1 deliberately has no backward compatibility path."
}
