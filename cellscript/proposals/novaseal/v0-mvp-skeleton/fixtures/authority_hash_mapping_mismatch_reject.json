{
  "name": "authority_hash_mapping_mismatch_reject",
  "description": "The lock args / declared authority identifier do not match the protected Cell authority. Even with an otherwise valid signature payload, the lock must reject before treating the signer as authorised.",
  "category": "negative",
  "acceptance_criteria_covered": [3, 6],
  "inputs": {
    "lock_args_authority_matches": false
  },
  "expected": {
    "result": "rejected",
    "failure_mode": "authority_hash_mapping_mismatch",
    "notes": "This keeps authority id, lock args, and witness pubkey binding as explicit checks instead of relying on coincidental 32-byte equality."
  }
}
