{
  "name": "policy_hash_mismatch_reject",
  "description": "The intent or the proposed new cell carries a different policy_hash than the old cell. Must be rejected. This is the on-chain enforcement of 'the package that governs this cell is immutable unless an explicit migration path exists'.",
  "category": "negative",
  "acceptance_criteria_covered": [7],
  "inputs": {
    "old_cell": { "policy_hash": "0xpolicy_v1" },
    "intent": { "policy_hash": "0xpolicy_v2" }
  },
  "expected": {
    "result": "rejected",
    "failure_mode": "policy_hash_mismatch",
    "notes": "Prevents silent upgrade or downgrade attacks via malicious intents."
  }
}
