{
  "name": "expired_intent_reject",
  "description": "The intent's expiry has already passed relative to the current block/timepoint. Must be rejected by both lock and type script.",
  "category": "negative",
  "acceptance_criteria_covered": [5],
  "inputs": {
    "intent": { "expiry": 100 },
    "current_timepoint": 200
  },
  "expected": {
    "result": "rejected",
    "failure_mode": "intent_expired",
    "notes": "Time-based safety valve. Important for key compromise recovery windows."
  }
}
