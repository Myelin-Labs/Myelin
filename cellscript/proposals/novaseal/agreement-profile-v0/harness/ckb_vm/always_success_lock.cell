module novaseal::agreement_profile::harness::always_success_lock;

// Arthur Zhang, 2026.
//
// HARNESS BOUNDARY:
// Local transaction-shape harness lock only. It deliberately accepts every
// spend so the VM harness can exercise the agreement type script without
// modelling borrower/lender locks. It is not part of the Agreement Profile
// runtime and should not be treated as wallet-authority evidence.
lock always_success() -> bool {
    verification
    return true
}
