{
  "schema": "novaseal-agreement-fixture-v0.1",
  "fixture": "nonce_max_increment_reject",
  "action": "repay_arithmetic_boundary",
  "expected": "rejected",
  "coverage": "local-arithmetic-boundary-covered + production-gate-covered",
  "reject_guard": "active.nonce < U64_MAX",
  "description": "Terminal nonce increment must reject when active.nonce is already U64_MAX."
}
