{
  "schema": "cellscript-ckb-vm-fixture-v1",
  "description": "CellScript script always-reject fail case - action returns 1, proving the harness correctly captures script failure",
  "entry_action": "verify_always_reject",
  "cellscript_source": "inline",
  "expected_result": "fail",
  "failure_mode": "always_reject",
  "ckb_transaction": {
    "script_args": "0x",
    "inputs": [
      {
        "capacity": 100000000000,
        "lock_script": "always_success",
        "type_script": null,
        "data": "0x"
      }
    ],
    "outputs": [
      {
        "capacity": 100000000000,
        "lock_script": "always_success",
        "type_script": "current_under_test",
        "data": "0x"
      }
    ],
    "cell_deps": [],
    "header_deps": [],
    "witnesses": []
  },
  "cellscript_metadata": {
    "primitive_compat": null,
    "note": "Uses verify_always_reject action which returns 1. This proves the CKB VM harness correctly captures script failure. The action is zero-argument so it gets a proper _start -> _cellscript_entry -> action chain."
  }
}
